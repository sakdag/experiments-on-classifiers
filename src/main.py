from src.supervised_learning import knn, decision_tree_regressor, k_neighbors_regressor, naive_bayes


if __name__ == '__main__':

    # Run KNN experiments with default settings
    knn.main()

    # Run KNN experiments with default settings
    k_neighbors_regressor.main()

    # Run KNN experiments with default settings
    decision_tree_regressor.main()

    # Run KNN experiments with default settings
    naive_bayes.main()
